#my_courses
  - begin
    .icon
      %h4 My Courses
      = image_tag PersonCourse.first.icon_url
    .content
      - if @my_courses and !@my_courses.empty?
        .scroll_list
          %ul
            - @my_courses.map{|c| Course.find(c)}.each do |course|
              %li= link_to course.name, course
      - else
        %p 
          You haven't added any courses, yet. You can do so on a course page.
  - rescue
    %h4 My Courses
    - if PersonCourse.first
      %p Please report an error with this block!
    - else
      %p There aren't any enrolments in the database, yet?
