- if review.published
  = link_to image_tag("actions/print.png"), person_event_url(@topic, event.eventable.events.creation.first, :format => "pdf")
- if review.attendance
  %h4 
    Attendance:
    = "#{review.attendance}%"
%h4 
  %b Review Summary:
