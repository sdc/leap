%img{:src => "https://chart.googleapis.com/chart?chs=200x48&cht=ls&chco=0077CC&chf=bg,s,00000000&chd=t:" + event.person.attendances.select{|a| a.week_beginning < attendance.week_beginning}.map{|a| a.att_year}.join(',')}
%dl
  %dt This week:
  %dd= "#{attendance.att_week}%"
  %dt Last three weeks:
  %dd= "#{attendance.att_3_week}%"

