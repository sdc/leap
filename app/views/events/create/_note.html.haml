= form_for @topic.events.new(:eventable_type => "Note") do |f|
  %h3
    Add a note
  .icon= image_tag "events/notes.png"
  = f.text_area :body
  = f.hidden_field :person_id
  = f.hidden_field :eventable_type
  = hidden_field_tag :person_id, @topic.mis_id
  .sumbit= submit_tag "Add Log"
