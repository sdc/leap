%h2 Add support history information
= create_event_form SupportHistory do |f|
  .span1= image_tag "events/support_histories.png"
  .span5
    = f.text_area :body, :class => "charcount", "data-maxchars" => "4000"
    %span{:id => "id_test"} 
      %h5
        You have <span id="support_history_body_chars" class="charcount_chars">4000</span> / 4000 characters remaining 

  .span3= select_tag "support_history[category]", options_for_select(Settings.support_history_categories.split(/,/))
  = add_event_button
