= form_for @topic.progression_reviews.new(:approved => false), :url => "/events" do |f|
  %h3 
    Learner approved to continue onto next level of course? 
    = f.select :approved, {"No" => false, "Yes" => true}
  #progression_reviews_reason_div
    If no, please select appropriate reason:
    = f.select :reason, Settings.progression_reviews_reasons.split(/,/), {}, {:activate_on => "progression_review_approved", :multiple => true}
  #progression_reviews_conds_div{:style => "display:none"}
    Conditional offer based on successful completion of learner's current course which should be informed by the Term 3 Review
  .sumbit{:style => "clear:both;float:right"}= submit_tag "Add"
  = f.hidden_field :person_id
  = hidden_field_tag :eventable_type, "ProgressionReview"
  = hidden_field_tag :person_id, @topic.mis_id


.online_help
  %h1 Continuing Learning Review
  %p Do stuff.
