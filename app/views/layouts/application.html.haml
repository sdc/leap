!!!
%html
  %head
    - if @topic
      %title= "#{@topic.name} : Leap"
    - else
      %title Leap
    = stylesheet_link_tag 'screen.css', :media => 'screen, projection'
    = stylesheet_link_tag 'print.css', :media => 'print'
    /[if lt IE 8]
      = stylesheet_link_tag 'ie.css', :media => 'screen, projection'
    /[if lt IE 9]
      = stylesheet_link_tag 'ie8.css', :media => 'screen, projection'
      = javascript_include_tag 'html5.js'
    = javascript_include_tag :defaults
    = javascript_include_tag "events.js"
    = javascript_include_tag "home.js"
    = csrf_meta_tag
  %body
    - if @affiliation == "staff"
      #top_bar
        .inner
          #new_search_link
            = link_to search_people_url do
              = image_tag "icons/search.png"
              New Search
          #notice
            Please Note: This is beta software. Changes you make will be temporary.
          #login_name
            logged in as
            = link_to @user.name, person_url(@user)
            = "(#{@affiliation})" unless @affiliation == "student"
              
    .container#main_container
      #side_pane
        - if @multi
          = render :partial => "layouts/multi_sidebar"
        - else
          = render :partial => "layouts/person_sidebar" if @topic
      #main_pane= yield
      #footer
        blah blah blah...
